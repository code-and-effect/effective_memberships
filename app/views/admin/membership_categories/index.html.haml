%h1.effective-admin-heading= @page_title

%p.text-right.effective-admin-actions
  = link_to 'New Membership Category', effective_memberships.new_admin_membership_category_path, class: 'btn btn-primary'

- @datatable ||= Admin::EffectiveMembershipCategoriesDatatable.new
= render_datatable(@datatable)
